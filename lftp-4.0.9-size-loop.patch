diff --git a/src/FileCopy.cc.old b/src/FileCopy.cc
index 9d8c95d..99f29b5 100644
--- a/src/FileCopy.cc.old
+++ b/src/FileCopy.cc
@@ -802,8 +802,8 @@ int FileCopyPeerFA::Do()
 	 return MOVED;
       }
       FileInfo *fi=info[0];
-      if(fi->Has(fi->SIZE))
-	 SetSize(fi->size);
+      SetSize(fi->size);
+
       if(fi->Has(fi->DATE))
 	 SetDate(fi->date);
       session->Close();
